using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IronAxeData : MeleeWeaponData
{
    public IronAxeData(string name, string description, int attack, float back, AttackType attackType, WeaponType weaponType, float radius, float speed, int maxCount) : base(name, description, attack, back, attackType, weaponType, radius, speed, maxCount)
    {

    }
}
